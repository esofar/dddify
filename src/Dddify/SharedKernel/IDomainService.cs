namespace Dddify.SharedKernel;

/// <summary>
/// Marks a type as a domain service.
/// </summary>
/// <remarks>
/// Domain services encapsulate domain logic that does not naturally belong to a single entity,
/// aggregate root, or value object, while still remaining part of the domain layer.
/// </remarks>
public interface IDomainService
{
}
